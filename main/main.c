#include <stdio.h>
#include <stdbool.h>
#include <SDL.h>
#include <SDL_ttf.h>
#include <global.h>

int main(int argc, char *argv[])
{
    bool is_running = true;
    
    do {

    }
    while (is_running);

    return 0;
}
